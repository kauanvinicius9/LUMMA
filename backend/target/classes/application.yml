spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lumma
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false
    # schemas: lumma
    # locations: classpath:db/migration

server:
  port: 8080